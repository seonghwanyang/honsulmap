# 직판 배너 광고 — 슬롯 스펙 & 운영 가이드

**최종 갱신:** 2026-07-14 · 담당 코드: `src/app/MapClient.tsx` (`AD_BANNERS`, `AdBannerSlot`)

## 1. 슬롯

| 항목 | 값 |
|---|---|
| 위치 | 지도 메인 — 실시간 근황 캐러셀 바로 아래 (1슬롯) |
| 표시 비율 | **1206 : 190 고정** (모든 배너 동일 크기 — 크리에이티브가 달라도 슬롯 점유 불변) |
| 최대 폭 | 480px (지도 가림 방지 — 넓은 화면에선 중앙 정렬) |
| 표기 | 우상단 `AD` 칩 |
| 클릭 | 해당 가게 패널 열림 (내부 이동 — `instagram_id`로 매칭) |

## 2. 로테이션 (2026-07-14 확정 · 업계 조사 반영)

- **방식**: 캐러셀식 상시 이동이 아니라 **"정지 → 짧은 슬라이드" 반복**
- **전환 형태**: **push 슬라이드** — 이전 배너가 왼쪽으로 밀려나가는 동안 다음 배너가 이어 붙어 들어옴 (전환 내내 두 광고 다 보임). **중간 회색/빈 프레임 없음** — 빈 프레임은 로딩 실패처럼 보이고 광고 노출 시간만 깎아서 쓰는 곳이 없음. 캐러셀 UX 가이드도 cross-fade보다 수평 슬라이드 권장
- **정지 노출**: `AD_ROTATE_HOLD_MS = 5000` (5초)
  - 근거: UX/접근성 가이드(W3C·NN/g 계열)는 슬라이드당 **최소 5~7초**(읽을 시간), 국내 커머스 앱 롤링배너 실측은 3~5초대 → 두 기준이 겹치는 5초 채택. 5초 초과 회전엔 일시정지 버튼 권장이라 5초가 컨트롤 없이 쓸 수 있는 스윗스팟
- **슬라이드 전환**: `AD_ROTATE_SLIDE_MS = 400` (업계 300~400ms 구간)
- **가속도(이징)**: `cubic-bezier(0.05, 0.7, 0.1, 1)` — Material 3 "emphasized decelerate". **최고 속도로 출발해 끝에서 부드럽게 감속**하는 커브. CSS 기본 `ease`는 출발이 느려 짧은 슬라이드에서 뚝 끊겨 보임 (1차 구현이 안 매끄러웠던 원인)
- **시작 순서**: 접속(마운트)마다 랜덤 → 광고주 간 첫 노출 균등
- **순환**: 마지막 배너 → 첫 배너로 무한 루프 (클론 트릭, 역방향 점프 없음)
- 백그라운드 탭에선 진행 정지 (`document.hidden` 가드), 배너 1개뿐이면 고정 노출
- GPU 합성: 트랙에 `will-change: transform`

## 3. 크리에이티브 규격

- **광고주 제출 가이드**: 1500×240 (`Business_advertisement/banner_guide_1500x240.png`) — 중앙 60% 세이프존, 상하 여백 금지
- **가이드와 다른 원고가 오면** (실사례 2건):
  - 포스터형(지문인식): CSS 세로 크롭 — `aspectRatio` + `objectPosition`으로 타이틀 밴드만 노출
  - 여백 있는 배너형(엮은이): PIL로 여백 제거 → 콘텐츠 축소 → **원본 배경색 1206×190 캔버스 재조판** 후 파일 자체를 규격화
- 파일 위치: `public/ads/*.jpg` (원본 보관: `Business_advertisement/`)

## 4. 게재 관리

- 게재 추가/제거 = `MapClient.tsx`의 `AD_BANNERS` 배열 수정 (배포 필요)
- 항목: `{ src, alt, imgStyle?, ig?, brand?, scope? }`
  - `ig`: 단일 지점 — 클릭 시 그 가게로 이동 (달밤 이태원)
  - `brand`: 다지점 브랜드 정규식(예: `/^nowavebar/`, `/^jimuninsik/`) — 클릭 시 **지도 화면 중심에서 가장 가까운 지점**으로 이동 (위치권한 불필요, viewBounds 중심 기준)
  - `scope`: **상품 2종** — `'national'`(전체광고, 전국 노출·기본값) / `'local'`(지역광고, 화면 중심 **30km 내에서만** 노출)
- **노출 순서**: 최초 지도 위치 기준 지역광고 필터 후 **최근접 광고부터** 로테이션 시작(랜덤 아님) → 5초마다 순환. 한 번만 계산해 팬해도 리셋 안 됨.
- 클릭 동작: 검색 클릭과 동일 — 대상 가게로 morph(zoom16, 클러스터 풀려 마커 단독) → 700ms 뒤 패널.
- **현재 게재 (4, 전부 national)**: 지문인식 `brand:/^jimuninsik/` · 엮은이 `brand:/^the_editor/` · 달밤 이태원 `ig:dalbam_seoul_itaewon` · 노웨이브 `brand:/^nowavebar/`
- 지역광고로 팔면 그 항목에 `scope:'local'` 한 줄 추가 → 근처 유저에게만. 반경은 `LOCAL_R`(MapClient.tsx, 기본 30km).
- ⚠️ 영업 시 **슬롯 수량(선착순 몇 자리인지)은 사장에게 언급 금지**

## 5. 추후 (필요해지면)

- 노출/클릭 측정 (GA4 `track()` — 배너별 impression·click)
- 게재 기간 자동 만료 (시작/종료일 필드)
- 유료 등급별 노출 가중치
- DB 관리화 (admin에서 배너 등록 — 지금은 배열 수정으로 충분)
